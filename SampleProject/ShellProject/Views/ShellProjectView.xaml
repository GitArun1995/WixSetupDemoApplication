<Window x:Class="ShellProject.Views.ShellProjectView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShellProject.Views"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/" 
        prism:ViewModelLocator.AutoWireViewModel="True"
        ResizeMode="NoResize"
    WindowState="Maximized"
    WindowStyle="None"
        Title="ShellProjectView" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Main content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header Region -->
        <ContentControl prism:RegionManager.RegionName="HeaderRegion" Grid.Row="0" Background="LightBlue" Height="50"/>

        <!-- Main Region -->
        <ContentControl prism:RegionManager.RegionName="MainRegion" Grid.Row="1" Background="WhiteSmoke"/>

        <!-- Footer Region -->
        <ContentControl prism:RegionManager.RegionName="FooterRegion" Grid.Row="2" Background="LightGray" Height="30"/>
    </Grid>
</Window>
