<UserControl x:Class="Header.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
            >
    <Grid Height="50" >
        <Border HorizontalAlignment="Stretch" Background="{DynamicResource Primary700}" CornerRadius="5" Margin="2,0.5,2,0">
            <!--<Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{DynamicResource Primary700}" Offset="0.9"/>
                    
                </LinearGradientBrush>
            </Border.Background>-->
            
        </Border>
    </Grid>
</UserControl>
